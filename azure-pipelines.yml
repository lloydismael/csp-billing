trigger:
- main

variables:
  # 1. Your Docker Hub connection
  dockerRegistryServiceConnection: 'Service Connection csp-billing' 
  
  # 2. Your exact repository path
  imageRepository: 'lloydismael12/csp-billing' 
  
  # 3. SAFE SYNTAX: Use format() to combine 'v6.' and the counter
  # This prevents "Expected mapping end" errors by keeping the expression clean
  tag: $[ format('v6.{0}', counter('version_v6', 1)) ]
  
  dockerfilePath: '$(Build.SourcesDirectory)/Dockerfile'

  # 4. Azure connections
  azureServiceConnection: 'Consumption Service Connection'
  webAppName: 'AzureConsumption'

stages:
- stage: Build
  displayName: Build and push stage
  jobs:
  - job: Build
    pool:
      name: 'Default' # Uses your self-hosted agent
    steps:
    - task: Docker@2
      displayName: Build and push to Docker Hub
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)
          latest

- stage: Deploy
  displayName: Deploy to Azure
  dependsOn: Build
  condition: succeeded()
  jobs:
  - job: Deploy
    pool:
      name: 'Default'
    steps:
    - task: AzureWebAppContainer@1
      displayName: 'Azure App Service Config & Deploy'
      inputs:
        azureSubscription: $(azureServiceConnection)
        appName: $(webAppName)
        # Quoting this string protects the colon and variable
        containers: '$(imageRepository):$(tag)'
        appSettings: '-WEBSITES_PORT 8000'